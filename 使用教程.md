# Rhino MCP 使用教程（Rhino 8 / Claude Code / Codex）

> 目标：让 AI 通过 MCP 稳定控制 Rhino 3D，完成建模、查询、截图等操作。

## 1. 环境准备

- Rhino 8（Windows）
- Python 3.10+
- 本仓库代码

建议先在命令行确认：

```bash
python --version
```

## 2. 安装 MCP 服务端

在仓库根目录执行：

```bash
pip install -e ".[dev]"
```

如果网络受限，至少保证可运行：

```bash
PYTHONPATH=src python -m rhino_mcp.server --mock
```

## 3. 安装 Rhino 插件脚本

执行：

```bash
python scripts/install_rhino_plugin.py
```

它会把 `src/rhino_plugin` 下脚本复制到 Rhino 可加载位置（按提示操作）。

## 4. 在 Rhino 8 中启动 Socket 插件

在 Rhino 命令行运行：

```text
_RunPythonScript "C:/.../socket_server.py"
```

成功后应看到类似：

- `RhinoMCP listening on localhost:9876`
- 客户端连接时会打印 `Client connected`

## 5. 启动 MCP 服务

新开一个终端：

```bash
rhino-mcp
```

如需指定地址：

```bash
RHINO_HOST=localhost RHINO_PORT=9876 rhino-mcp
```

## 6. 连通性检查（推荐）

```bash
rhino-mcp-health --host localhost --port 9876
```

期望返回 JSON，含：

- `status: success`
- `result.rhino_connection: connected`

## 7. 先用 Mock 模式自检（不依赖 Rhino）

```bash
rhino-mcp --mock
```

并运行测试：

```bash
PYTHONPATH=src pytest -q
```

## 8. Claude Desktop / 其他 MCP 客户端配置

示例（`claude_desktop_config.json`）：

```json
{
  "mcpServers": {
    "rhino": {
      "command": "rhino-mcp",
      "args": [],
      "env": {
        "RHINO_HOST": "localhost",
        "RHINO_PORT": "9876"
      }
    }
  }
}
```

## 9. 常见问题排查

### 9.1 无法连接 9876

- 确认 Rhino 里插件脚本已运行。
- 确认端口没被占用。
- 确认 host/port 与 MCP 端一致。

### 9.2 偶发超时

- 增大工具调用 `timeout`。
- 避免一次执行超大脚本，分步执行。
- 先 `health_check` 再执行复杂布尔运算。

### 9.3 AI 调用了工具但 Rhino 无变化

- 查看 Rhino 命令行输出。
- 查看 MCP 服务端日志（JSON 格式）。
- 先试 `create_sphere` 这类最小命令验证链路。

## 10. 推荐工作流

1. `rhino-mcp --mock` 跑测试确认工具链可用。  
2. Rhino 内启动 `socket_server.py`。  
3. 终端执行 `rhino-mcp-health` 检查连接。  
4. 再让 Claude/Codex 进行真实建模任务。  

---

如果你希望，我可以下一步给你补一份“从零到生成参数化模型”的完整提示词模板（适合 Claude Code / Codex 直接调用）。
